# Local skills installation (task 005)

Pinned source:
- Repository: https://github.com/vercel-labs/agent-skills
- Selected skills: react-best-practices, composition-patterns, web-design-guidelines, react-view-transitions
- Pin reference used for this task: `pinned-for-task-005` (recorded in each `SKILL.md`)

Reproducible update procedure:
1. Ensure `.claude/skills` exists in the project root.
2. Sync selected skill folders from the pinned source reference.
3. Verify each selected skill contains `SKILL.md` metadata including `source_repo`, `source_path`, and `source_ref`.
4. Validate with: `ls -la .claude/skills` and `find .claude/skills -maxdepth 2 -name SKILL.md`.
